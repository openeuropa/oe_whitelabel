{#
/**
 * @file
 * Theme override to display a media item.
 *
 * @see ./core/themes/stable/templates/content/media.html.twig
 */
#}

{% if files %}
  {% for _file in files %}
    {{ pattern('file', {
      file: _file,
      translations: _file['translations'],
    }) }}
  {% endfor %}
{% endif %}
