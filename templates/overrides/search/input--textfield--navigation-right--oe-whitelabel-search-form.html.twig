{#
/**
 * @file
 * Theme for the navigation_right search input textfield element.
 *
 * @see ./core/modules/system/templates/input.html.twig
 */
#}
{%
  set classes = [
  'form-control',
  'border-start-0',
  'rounded-0',
  'rounded-start',
  'form-control',
  attributes.hasClass('error') ? 'is-invalid',
]
%}
<input{{ attributes.addClass(classes).setAttribute('placeholder', 'Search'|t).removeClass('form-text') }} />{{ children }}

